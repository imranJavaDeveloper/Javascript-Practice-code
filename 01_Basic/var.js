/**
 * String
 * boolean
 * undefined
 * Null
 * Bigint
 */

// Example
  let fullName="md furkan alam";
  let age =22;
  let address="Nawada Bihar"
  const pincode=805110;

  // let keyword 

  {
    let a=55;
    console.log(a);
  }

  console.log(a);

   
   x=null;
   y=undefined;

   // show all deatil
   console.log(fullName);
   console.log(age);
   console.log(address);
   console.log(pincode);

   /* variable Rule

   1. variable name is case sensitive
   2. only latter ,digit,underscore(_) and $ symbol is allow.(not even space)
   3. only latter and underscore(_) or $ should be first character
   4. keyword not allow */



   


   
